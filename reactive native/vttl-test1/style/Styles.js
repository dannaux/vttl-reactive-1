import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  ranking: {
    fontWeight: 'bold',
    fontSize: 40
  },
  playerName: {
    fontSize: 30
  },
  matchen: {
    fontSize: 20
  }
});
